<% if @pages.empty? %>
  <p>Doesn't look like you've got any pages.</p>
<% else %>
  <ul>
  <% @pages.each do |page| %>
    <li><a href="/edit/<%= page._id %>"><%= page.title || 'untitled' %></a> <a href="/delete/<%= page._id %>">delete</a></li>
  <% end %>
  </ul>
<% end %>